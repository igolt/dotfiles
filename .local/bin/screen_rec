#!/bin/sh
if [ ! -z "$XDG_DATA_HOME" ]; then
    SAVE_DIR="$XDG_DATA_HOME/screen_rec"
else
    SAVE_DIR="$HOME/.screen_rec"
fi
mkdir -p "$SAVE_DIR" || SAVE_DIR="$HOME"

ffmpeg -video_size $SCREEN_RES -framerate 25 \
    -f x11grab -i :0.0 "$SAVE_DIR/$(date +"%Y-%m-%d-%H-%M-%S").mp4"
